// inputs:
// -background city-id(hint)
// -one of img from 0 to 6 for hangman state (head, legs, arms)

// outputs:
// -picture of city on background wall
// -picture of one of the seven states(head, arm, leg)

// 2/3 width of display
// 100% height of display